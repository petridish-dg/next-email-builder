export default function OneUpPreview({imageURL, heading, intro, buttonText, buttonURL}) {
    return (
        <div>
            <p>OneUpPreview</p>
            <p>imageURL: {imageURL}</p>
            <p>heading: {heading}</p>
            <p>intro: {intro}</p>
            <p>buttonText: {buttonText}</p>
            <p>buttonURL: {buttonURL}</p>
        </div>
    );
}